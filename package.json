{
  "name": "erc721ish-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ cryptovoxels/erc721ish-subgraph",
    "create-local": "graph create --node http://localhost:8020/ cryptovoxels/erc721ish-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ cryptovoxels/erc721ish-subgraph",
    "deploy-local": "graph deploy --product hosted-service --node http://localhost:8020/ --ipfs http://localhost:5001 cryptovoxels/erc721ish-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.1",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}
